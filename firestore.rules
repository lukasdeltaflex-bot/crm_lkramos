rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // MODO DEBUG: PERMISSÃO TOTAL PARA TESTES DE CONEXÃO
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
