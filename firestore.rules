rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 🛡️ PERMISSÃO TOTAL V25: Garantia de acesso para evitar bloqueios em ambiente de nuvem
    match /{document=**} {
      allow read, write: if true;
    }
  }
}